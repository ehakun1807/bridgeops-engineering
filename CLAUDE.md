# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

- `npm run dev` — Vite dev server on **port 3000** (not the default 5173). Configured in `vite.config.ts`.
- `npm run build` — production build to `dist/`.
- `npm run preview` — serve the built `dist/` locally (port 4173).
- `npm run lint` — `tsc --noEmit` (TypeScript type-check; no ESLint).
- `npm test` — Jest. Test files live in `__tests__/` and match `**/__tests__/**/*.test.ts`.
- Run a single test: `npx jest __tests__/<file>.test.ts` or `npx jest -t "<test name>"`.
- `npm run deploy:rules` / `deploy:indexes` / `deploy:firestore` — Firebase deploys.

`GEMINI_API_KEY` must be set in `.env.local` for local dev and in Vercel's project env vars for prod. The key is **server-only** — read by the `api/*.ts` handlers via `process.env.GEMINI_API_KEY`. It is NOT injected into the client bundle (the old `define` block was removed — see 2026-05-13 session log). Never reintroduce it into `vite.config.ts`'s `define` or any client-side code.

## Architecture

**Flat layout — no `src/` directory.** Every component, client, and helper sits at the project root. The path alias `@/*` resolves to the project root (both Vite and tsconfig). Imports must include explicit `.tsx` / `.ts` extensions (`allowImportingTsExtensions: true`).

**Entry & routing.** `index.tsx` mounts `App.tsx`. `App.tsx` implements hash-based routing via `VIEW_TO_PATH` / `PATH_TO_VIEW` maps and the `View` union type in `types.ts`. Heavy views (`Dashboard`, `RampScoreTool`) are `React.lazy`-loaded to keep the marketing-home bundle lean. Adding a new view means: extend the `View` union in `types.ts`, register it in both maps + `VIEW_TITLES` in `App.tsx`, and add the lazy import.

**Two surfaces in one app.**
1. *Marketing site* (Hero, Services, Pricing, MethodologyLayers, ContactForm, etc.) — public.
2. *Admin / SaaS dashboard* (Dashboard → ProjectDeepDive → AdvancedToolsModal → BOMAnalyzerTool [user-facing label "Alt BOM"] / QuoteCompareTool / DocGuardTool [user-facing label "Doc Guard"], plus RampScoreTool, ScoreHistoryPanel, GateDeliverablesModal, TaktStudyTool, etc.) — gated by Firebase auth.

**ProjectDeepDive tab strip (2-tier).** Primary row = General Info + 4 score buckets (full card chrome, live %). Secondary strip below = AI Analysis / Studies / History (lightweight pills, separate `layoutId="deepdive-utility-underline"` so the active highlight doesn't morph between rows). Adding a new utility tab = append to the `utilityTabs` array around `ProjectDeepDive.tsx:1465`; no JSX duplication needed.

**Auth & admin gating.** `firebase.ts` initializes the Firebase app from `firebase-applet-config.json` and exports `auth` + `db` (Firestore). `config.ts` defines `ADMIN_EMAIL` and `isAdminUser()` — this is a UX gate only; the real security boundary is `firestore.rules` (`isAdmin()`). Keep `ADMIN_EMAIL` in `config.ts` and the rules file in sync.

**AI integrations.** `aiClient.ts`, `coachClient.ts`, and `docGuardClient.ts` are the front-end clients. The `api/` folder (`ai-analyze.ts`, `ai-coach.ts`, `find-equivalent.ts`, `docguard.ts`, `docguard-upload-token.ts`) holds serverless handlers that wrap Gemini. All five enforce the same admin gate: Firebase ID-token signature verify against Google's RS256 public keys, reject anything not matching `ADMIN_EMAIL` with `email_verified==true`. The two `docguard*` handlers also use Vercel Blob for client→server PDF transfer (see gotcha below).

**Domain logic / generators.** `rampGroups.ts`, `productSegments.ts`, `productStandards.ts`, `templates.ts`, `indicators.ts`, `pptxGenerator.ts` (PPTX export via `pptxgenjs`), `taktMath.ts` (pure IE math — takt, mean, stdDev, CV, normalized/standard time, bottleneck, line balance loss, capacity verdict, validation gates), `taktXlsx.ts` (xlsx workbook builder for time-study export, uses SheetJS), `utils/docGuardPdf.ts` (annotated-PDF builder via `pdf-lib` — extends each annotated page with a right-side findings sidebar, appends a verdict-coded summary page), `utils/docGuardPdfHelpers.ts` (pure helpers — `wrapText`, `groupByPage`, `sortFindingsForSummary` — no `pdf-lib` import so they're directly testable), and `notify.ts` are pure-ish modules consumed by the dashboard tools.

**Firestore collections.** `users`, `projects`, `scores`, `tasks`, `leads`, **`taktStudies`** (one doc per time-study; `userId + projectId` scoped), **`docGuardAudits`** (one doc per PDF audit run; `userId`-scoped, project-agnostic for v1, optional `projectId` field reserved). When a takt study is marked completed, `TaktStudyTool` mirrors a snapshot to `projects/{id}.taktSummary` (`{ studyId, studyName, taktSec, bottleneckSec, balanceLoss, capacity, completedAtMs }`) so the project header pill + future RampScore consumers can read the verdict without scanning the whole collection. Drafts never write the mirror; Reopen / Delete leave it alone (point-in-time snapshot). `docGuardAudits` are immutable historical records (`allow update: if false`) — re-running an audit creates a new doc; the corresponding composite index `(userId asc, createdAtMs desc)` is declared in `firestore.indexes.json`.

**Testing.** `jest.config.*` uses `ts-jest` + `jsdom`. Note `testMatch` is `*.test.ts` only — `.tsx` test files will be ignored. Setup file: `jest.setup.js`.

## Repository gotchas

- **`.claude/worktrees/<branch>/` contains full duplicates** of source files (it's a git worktree for a feature branch). **Never edit files there** — Vite doesn't import from it, but it shows up in `Grep` results and is misleading. Always edit at the project root.
- **`dist/` may exist locally** even though it's `.gitignore`d. If you see a stale build, the dev server on port 3000 is the source of truth — anything else (preview, deployed site) needs `npm run build` first.
- **Hash routing only.** Don't introduce `react-router-dom` or path-based routing without updating the routing logic in `App.tsx`.
- **No ESLint.** "Lint" means `tsc --noEmit`. Type errors are the lint signal.
- **Vite stale-cache symptom**: if a code edit isn't visible in the browser even after reload, kill the dev server and run `rm -rf node_modules/.vite dist` before restarting `npm run dev`. Vite's transform cache occasionally serves old compiled output.
- **Port drift**: `npm run dev` will silently fall back to 3001/3002/etc. if 3000 is occupied by a previous orphaned process. Check the terminal's `Local: http://localhost:XXXX` line and kill stragglers with `lsof -ti:3000 | xargs kill -9`.
- **Multi-database Firestore deploys lie.** `firebase.json` uses the array form for `firestore` because the project runs on a named database (`ai-studio-c787093b-8e5e-45dc-8458-6bfe3ae5539d`, not `(default)`). `npm run deploy:rules` will print **`✔ Deploy complete!`** even when nothing was actually written to the named database — verify via Firebase Console → Firestore → Security; if the "Last published" timestamp didn't change, the deploy lied. **Reliable workaround:** open the local `firestore.rules`, copy all, paste into the Console rules editor (Cmd+A → Delete → Cmd+V to avoid appending to the existing rules), click Publish. CLI flags worth trying first: `npx firebase deploy --only firestore --force --project gen-lang-client-0703668573`.
- **`.firebaserc` is required.** Without it, every Firebase CLI command errors with "No currently active project." File maps `default` → `gen-lang-client-0703668573`. Should be checked in.
- **Vercel function body limit is 4.5MB.** Any endpoint that takes a file in a JSON body via base64 will 413 once the file passes ~3MB raw (base64 inflates by ~33%, plus JSON envelope). `/api/docguard` was originally written this way and broke on an 8MB upload. Fix: `/api/docguard-upload-token` issues a Vercel Blob client upload token, the browser uploads directly to Blob (no function in the middle, no body limit), then the client sends only the Blob URL to `/api/docguard`. The handler fetches the URL server-side, encodes to base64, calls Gemini, and `del()`s the blob in a `finally` block. Auth flows via `clientPayload` because `@vercel/blob/client.upload()` doesn't forward arbitrary headers — `onBeforeGenerateToken` reads it back and runs the Firebase admin verify. The Blob store must be **public** access (the audit handler fetches via plain `fetch()` with no signed creds); URLs are deliberately short-lived (60s token validity) and deleted post-audit. **Required env var: `BLOB_READ_WRITE_TOKEN`** — auto-injected when a Blob store is linked to the Vercel project; nothing manual to set.
- **`.claude/worktrees/<branch>/` shows up as a phantom submodule.** The worktree path got registered in the parent repo's git index as a gitlink (the format submodules use), but there's no `.gitmodules` file. GitHub Desktop displays "Submodule changes" warnings even though it's not a real submodule. Fix once: `git rm --cached .claude/worktrees/<branch>` then `echo ".claude/worktrees/" >> .gitignore`. Never re-add the worktree to tracking. Independent issue from the Jest/grep duplicate-file gotcha already documented above — both have the same root (worktree at the project root) but different fixes.

## Session log

### 2026-05-01
- **UI fix:** changed `Advanced Tool` → `Advanced Tools` button label in `ProjectDeepDive.tsx` (line 1396, footer toolbar next to Cancel Project / Mark Completed).
- **Debugged stale-cache issue** while verifying that fix — root cause was Vite's `node_modules/.vite` cache plus an orphan dev-server holding port 3000. Documented as gotchas above.
- **Auth/security audit** of the AI Tool. Confirmed 4 independent layers, all hardcoded to `ehakun1807@gmail.com`:
  1. `AuthModal.tsx` — Google sign-in deletes non-admin Firebase Auth account on the spot; email/password path is login-only (no signup wired).
  2. `App.tsx` dashboard route — renders "Private Beta" lockout for any non-admin even if signed in.
  3. `firestore.rules` — `isAdmin()` checks role==admin OR email==ADMIN_EMAIL with `email_verified==true`. Per-collection owner/admin gating on projects/scores/tasks/users; `leads` is public-create / admin-read.
  4. `api/ai-analyze.ts`, `api/ai-coach.ts`, `api/find-equivalent.ts` — verify Firebase ID token signature against Google's RS256 public keys, reject anything not matching `ADMIN_EMAIL` + `email_verified`.
- **Open follow-ups:** (a) run `npm run deploy:rules` so production enforces current `firestore.rules`; (b) review Firebase Console → Authentication → Users for any unexpected accounts; (c) optionally disable Email/Password provider in Firebase Console since only Google is used in practice.

### 2026-05-02
- **New feature: TaktStudyTool** (Studies tab in `ProjectDeepDive`). Per-project time-study workspace: list of studies with status badges, in-place form with product title + 200-char comment + takt inputs (shift / breaks / demand → live takt seconds). Persists to `taktStudies` Firestore collection (rules added to `firestore.rules` mirroring the `scores` pattern with explicit `isValidTaktStudy` validator).
- **Step capture.** Compact rows with `+/−` zoom toggle. Expanded panel exposes step description (≤500 char, with counter), multi-cycle stopwatch (snap-back; Start / Lap / Reset + manual entry; observation chips with per-cycle delete; 100ms tick), live μ/σ/CV/n + standard-time readout, rating (Westinghouse, default 100%), allowance (PFD, default 12%), and VA / NVA / Wait classification with hover tooltips explaining each. Pure math is in `taktMath.ts` — Jest suite at `__tests__/taktMath.test.ts` covers takt, mean / stdDev / CV (sample n−1), normalized + standard time, bottleneck, total cycle, line balance loss, capacity verdict, and validation gates.
- **Lifecycle.** Draft → Complete & Validate → Completed (locked, can be Reopened). On Complete, `TaktStudyTool` runs `validateStudyForCompletion` and surfaces gaps inline. **Currently all gates are advisory (warn)** — completion is allowed even with a single observation per step, per explicit user request to remove blockers; the 5-cycle / 10-cycle / CV-0.25 thresholds still surface as warnings before and after Complete (advisories panel inside the VerdictPanel).
- **Verdict + visualization.** Completed studies render `VerdictPanel`: color-coded verdict bar (green / yellow / red from `capacityVerdict`), validated takt + bottleneck + total cycle + line balance loss + headroom %, plus a Yamazumi chart (inline SVG, one bar per step, height = standard time, color = VA / NVA / Wait, dashed red takt line, slate outline on bottleneck).
- **xlsx export.** `taktXlsx.ts` builds a 2-sheet workbook (Summary + Steps with cycle observations joined CSV-style). Download button in both the open form and per-row in the studies list.
- **Dynamic takt panel.** The takt-time card inside the form now adapts as steps come in: top band shifts color to match capacity verdict (slate "awaiting steps" → emerald / amber / red), and once steps exist the card surfaces bottleneck + headroom % live so the observer sees the gap forming during capture, not just after Complete.
- **RampScore wiring.** On Complete, `TaktStudyTool` writes `taktSummary` to the project doc (`{ studyId, studyName, taktSec, bottleneckSec, balanceLoss, capacity, completedAtMs }`). `ProjectDeepDive` header shows a colored "Capacity ●" pill in the existing pill row. Full integration into `RAMP_GROUPS` scoring (so capacity moves the overall readiness number) is **not** done yet — that's a follow-up.
- **Rename: BOM Analyzer → "Alt BOM"** in user-facing surfaces (`AdvancedToolsModal.tsx` modal title + tile heading; `Dashboard.tsx` Tools button tooltip + comment). New tile description: *"Find a qualified second-source alternate for every line on your BOM"* — leads with the output, not the verb. Internal identifiers (`BOMAnalyzerTool.tsx` filename, component name, `'bom-analyzer'` selector key, integration test file, `docs/superpowers/`) intentionally **not renamed** to keep the diff small.
- **Tab strip restructure.** `ProjectDeepDive` tab row was 8 cells (General Info + 4 score buckets + AI / Studies / History) which read as a flat peer set. Split into: primary 5-cell grid for the work surfaces, secondary thin pill strip for the 3 utility tabs. Driven by a `utilityTabs` array at `ProjectDeepDive.tsx:~1465` so adding a 4th utility (Coaching, Reports, Notes…) is a one-line append. Independent `layoutId` so the active underline doesn't try to morph between rows.
- **Validation relaxation.** Demoted every `level: 'block'` in `validateStudyForCompletion` to `'warn'` per user request. The "Cannot complete yet" banner now requires at least one true blocker before rendering (future-proofs the UI if hard gates come back). Tests rewritten + a new "single observation still warns" case added.
- **`.firebaserc` added.** Maps `default → gen-lang-client-0703668573`. Without this every Firebase CLI command errors out. Should be committed.
- **Firebase deploy debugging saga.** Phase 1 follow-up (a) from May 1 — deploying updated `firestore.rules`. The CLI repeatedly printed `✔ Deploy complete!` while NOT actually writing to the named database (Console "Last published" stayed at Apr 17). Worked around by **manually copy-pasting `firestore.rules` into the Firebase Console's Security tab and clicking Publish**. Documented as a gotcha (see above) so future sessions don't waste time on the same wall. After the manual publish, taktStudies writes started succeeding.
- **Open follow-ups (after today):**
  1. Wire `taktSummary.capacity` into `RAMP_GROUPS` so capacity actually moves the overall readiness score (not just a header pill). Likely a new sub-item in the Manufacturing group.
  2. Investigate the multi-database CLI deploy bug — `firebase-tools` 13.23.0 may be too old; bumping to a newer version is worth trying before relying on the manual-paste workaround long-term.
  3. Carry-over from May 1: review Firebase Console → Authentication → Users for any unexpected accounts; optionally disable Email/Password provider since only Google is used.
  4. Internal rename of `BOMAnalyzerTool` → `AltBOMTool` at the file/component level if the SaaS rename sticks. Cascades through imports + the integration test.

### 2026-05-05
- **New feature: Doc Guard** (renamed mid-build from `DocGuard` → `Doc Guard` at user-facing surfaces only — files / components / Firestore collection stay `docGuard*`, mirroring the BOMAnalyzer→"Alt BOM" precedent). Tile in `AdvancedToolsModal` next to Alt BOM and Quote Compare. Project-agnostic for v1.
- **Purpose.** Audit a manufacturing PDF (SOP, work instruction, assembly procedure) for grammar/typos, step numbering, GMP format/structure, DFA-style logical order, and image clarity. Returns short, actionable comments — capped at 200 chars per finding, max 50 findings — plus an annotated PDF for the author.
- **Stack.** Front-end client `docGuardClient.ts` + `DocGuardTool.tsx`. Two serverless handlers in `api/`: `docguard-upload-token.ts` (issues Vercel Blob client upload tokens) and `docguard.ts` (fetches the Blob, calls Gemini 2.5 Flash with `inlineData`/`mimeType` multimodal, returns normalized findings + summary). Annotated-PDF builder is `utils/docGuardPdf.ts` (pdf-lib); pure helpers in `utils/docGuardPdfHelpers.ts` are tested via `__tests__/docGuardPdfHelpers.test.ts` (11 cases — wrap edge cases, page grouping, severity sort).
- **Annotation strategy.** Each annotated page gets a 170pt-wide right-side sidebar (page is `setSize`-grown so original content is never covered) listing findings with severity-colored dot + category tag + short comment. A summary page is appended at the end with a verdict pill (PASS / MINOR ISSUES / MAJOR ISSUES) and the full sorted findings list. Downloaded as `<original_stem>__doc_guard.pdf`.
- **Persistence.** New collection `docGuardAudits` — one doc per audit, `userId`-scoped, project-agnostic for v1 (optional `projectId` reserved). Immutable (`allow update: if false`); re-run = new doc. Composite index `(userId asc, createdAtMs desc)` declared in `firestore.indexes.json` — required, the history list won't load until it's "Enabled" in the Firebase Console.
- **Vercel Blob upload pivot.** Phase-1 design used inline base64 in the JSON request body. First real test (8MB PDF) hit Vercel's 4.5MB function body limit and 413'd. Refactored to a two-hop upload: client → Blob → server-side fetch → Gemini → `del()` the blob in `finally`. Now handles up to 15MB PDFs (Gemini's `inlineData` ceiling). Auth flows via `clientPayload` on the `@vercel/blob/client.upload()` call because the library doesn't forward custom Authorization headers; `onBeforeGenerateToken` reads it back and runs the same Firebase admin verify as the other handlers. Documented as a gotcha for future tooling that takes binary uploads.
- **Field-naming bug.** First Gemini call returned generic `502 ai service error`. Cause: `inline_data` / `mime_type` (snake_case) instead of `inlineData` / `mimeType` (camelCase). The v1beta REST API is sometimes lenient but not for multimodal PDF content. Fixed; also updated the 502 handler to surface the upstream Gemini message + status to the browser instead of swallowing it (was hiding the root cause).
- **Phantom-submodule cleanup.** GitHub Desktop blocked the first commit with "Submodule changes" — `.claude/worktrees/hopeful-pike-2d1692/` was registered in the index as a gitlink with no matching `.gitmodules`. Fixed with `git rm --cached` + `.gitignore` entry. Documented as a gotcha.
- **Firestore rules + indexes.** Added `isValidDocGuardAudit` validator and `match /docGuardAudits/{auditId}` block (immutable, same get+list split as the other collections). Composite index added to `firestore.indexes.json`. Both must be deployed — and per the existing multi-database lie gotcha, verify in the Firebase Console rather than trusting `✔ Deploy complete!`.
- **Renamed mid-build.** "DocGuard" → "Doc Guard" at: AdvancedToolsModal title + tile heading, sign-in error toast, intro paragraph, annotated-PDF sidebar header, summary page title, generic API error, and the `__doc_guard.pdf` filename suffix. Internal identifiers (file names, component name `DocGuardTool`, selector key `'doc-guard'`, collection `docGuardAudits`, comments) intentionally NOT renamed. Same precedent as BOMAnalyzer → "Alt BOM".
- **Open follow-ups (after today):**
  1. **Pivot from auditor → editor.** User flagged interest in proactive fix-suggestions with track-changes-style review (Accept/Reject per finding) instead of just flagging issues. Architecture sketch already drafted (AI returns patches not just findings; in-app review screen; two output paths — marked-up PDF for surgical text fixes, Track Changes DOCX for structural rewrites). Big enough to be its own session.
  2. **Internal rename** of `DocGuardTool` / `docGuard*` files to match the user-facing "Doc Guard" if the SaaS name sticks. Cascades through imports, Firestore collection name (data migration needed!), test file. Same family as the BOMAnalyzer→AltBOMTool follow-up.
  3. **Wire Doc Guard from `ProjectDeepDive`** with a `projectId` flowing through `AdvancedToolsModal` so audits attach to projects (the `projectId` field on `docGuardAudits` is already reserved in the rules + UI).
  4. **Optional Jest cleanup**: add `testPathIgnorePatterns: ['/node_modules/', '/.claude/worktrees/']` to `jest.config.*` so `npm test` stops failing on stale worktree tests + the haste-map collision warning. Pre-existing issue, not blocking.

### 2026-05-06
- **Template-aware gate UI.** The Gate Readiness strip and the deliverables-level gate indications (the "Due by · CR · PDR · …" filter pill bar, per-row `dueBy` badges, the gate `<select>` on custom deliverables, and the gate `<select>` in the add-custom row) are now suppressed for non-Full-Ramp projects. Driven by a single derived flag `gatesEnabled = (project.templateId ?? 'full_ramp') === 'full_ramp'` at the top of `ProjectDeepDive`, threaded through `MetricRow` → `DeliverableChecklist`. PCBA / Mechanical / Pilot / Custom all hide the gate UI; legacy projects with no `templateId` default to Full Ramp (preserves original behaviour). Existing `dueBy` data on saved deliverables is left untouched on disk — UI-only suppression — so switching a project's template back would restore everything as it was. The "Current gate" picker on the General Info tab is intentionally NOT hidden (it's project metadata, not gate-readiness UI).
- **Value-item scores follow deliverable completion.** Previously `kind: 'bar'` items already auto-derived from deliverables (the slider is replaced by `done/total × 100`), but `kind: 'value'` items were purely numeric — adding a custom deliverable to MTBF/FPY/scrap/etc. wouldn't move the bar. Implemented a 50/50 blend in `rampGroups.ts`:
  - `scoreForItem(item, value, deliverablePct?)` — when `deliverablePct` is present and the item is value-kind, returns `Math.round((numericScore + clamp(deliverablePct, 0, 100)) / 2)`. Bar items ignore `deliverablePct` so we don't double-count (their `value` is already deliverable %).
  - `scoreForGroup` and `scoreForProject` got an optional 4th arg `deliverableScores?: Record<string, number>` and pass it through. **Backward-compatible** — Dashboard / PortfolioHeatmap / aiClient call sites still work unchanged (they pass `undefined`, get legacy numeric-only scoring).
  - In `ProjectDeepDive`, derived a `deliverableScores` map via `useMemo` (one entry per value-kind item with `deliverableCountsForItem(...).total > 0`) and threaded it into every internal rollup call: `overall`, `groupScores` for the history snapshot, the primary tab pill cards, and the active group card. So the row chip + slim bar + group % + overall % all shift in lockstep when a deliverable is added/checked/waived.
  - In `MetricRow`'s value branch, added a small "X/Y delivered" chip next to the slim bar (mirrors the bar-kind "Auto · X/Y delivered" chip). Tooltip explains the 50/50 blend.
- **Known inconsistency.** Dashboard / PortfolioHeatmap / aiClient still pass `undefined` for `deliverableScores`, so their list-view rollups for value items reflect numeric-only scoring while ProjectDeepDive shows the blended score. Bounded discrepancy — max ~50% delta in extreme cases. Cleanest fix is to extract a shared `deliverableScoresForProject(project)` helper (lives near `scoreForGroup` so it's importable everywhere) and pass it from those call sites; deferred so the change stays scoped.
- **New feature: MEETINGS** (per-project meetings log). New utility tab in `ProjectDeepDive`'s secondary strip, sitting between **Studies** and **History**. Same chrome as `TaktStudyTool` (slate-900 header, list view + form view, blue New button) but a simpler model — always editable, no draft/completed lifecycle. User asked for "reopen whenever," so we skipped the validation/lock ceremony. New file `MeetingsTool.tsx` (~470 lines), tab id `__meetings__`, threaded into `ProjectDeepDive` like `TaktStudyTool`.
- **Form fields** (per spec): Date (UTC-anchored `<input type="date">`, defaults to today), Scope/Title (200 char, required), Attendees (free-text textarea, 500 char, comma-separated), Type (Internal / External dropdown — internal default, external for vendors/customers/partners/auditors), Discussion Notes (1000 char textarea), Action Items (1000 char textarea). All four char-capped surfaces have live counters in the bottom-right.
- **List view** shows date · Internal/External chip (blue / amber) · "Action items" badge if any · title · attendee preview. Click row to reopen, trash icon to delete (with confirm). Empty state guides the user to "New Meeting."
- **Persistence: `meetings` Firestore collection.** `userId + projectId` scoped, owner-isolated. `isValidMeeting` validator + `match /meetings/{meetingId}` block in `firestore.rules` mirrors the `taktStudies` pattern, with **update** allowed (not immutable like docGuardAudits — meetings stay editable). Composite index `(userId asc, projectId asc, dateMs desc)` added to `firestore.indexes.json`. Same multi-database deploy lie applied — rules had to be **manually pasted into the Firebase Console** for the named DB before saves stopped 503'ing with "Missing or insufficient permissions."
- **AI Analysis bugfixes (`api/ai-analyze.ts` + `aiClient.ts`).** Custom-template projects were returning a generic `ai service error`. Two compounding bugs:
  - **Bug 1: empty-scope crash.** Custom template starts with `enabledItemIds: []` so every metric is disabled. `aiClient.buildSnapshot`'s `.filter((g) => g.enabledCount > 0)` then strips all 4 groups → `groups: []` reaches the handler → Gemini gets a degenerate prompt with the "Parameter Groups (enabled metrics only):" header followed by nothing → upstream rejects → 502.
  - **Bug 2: error masking.** `api/ai-analyze.ts:393` was the only AI handler still swallowing the upstream Gemini status + body — the May 5 fix that landed in `docguard.ts` and `quote-compare.ts` had skipped this file. So we couldn't see the real cause.
  - Fix: pre-flight check in `aiClient` (clear "no metrics in scope — open scope and enable a metric" message, no wasted round-trip), server-side guard for the same condition (defense in depth), and ported the upstream-leak pattern (`safeDetail` with `AIza…` keys stripped, max 300 chars, `error + detail` envelope) so future 502s show the actual cause. `aiClient` now appends `detail` to the thrown error message.
- **Retry+fallback in `api/ai-analyze.ts`.** After fixing the masking we immediately hit `503 high demand` (Gemini overloaded). `ai-analyze` was the only AI handler without the retry+fallback ladder that's been in `ai-coach` / `find-equivalent` / `quote-compare` since their first ship. Ported the same pattern: primary `gemini-2.5-flash` with up to 2 backoff retries (500ms, 1500ms) on 429/500/502/503/504/network, then ONE fallback to `gemini-2.5-flash-lite` (different capacity pool — lite usually answers when flash is overloaded). Worst-case wall time ~14s, fits inside Vercel's 30s function limit. Friendlier 503 message specifically: "AI service is overloaded right now — please try again in a minute."
- **Closed the deliverable-blend gap (follow-up #1 from earlier today).** Dashboard was showing 21% on a project the deep-dive showed as 12%. Two pieces, both required to fully close:
  - **`deriveDeliverableScores(deliverables)` in `rampGroups.ts`** — returns `id → pct` for value-kind items. Threads into `scoreForGroup` / `scoreForProject` so list-view rollups blend deliverable completion the same way the deep-dive does. Uses a duck-typed `DeliverableStateLike` interface so `rampGroups` stays UI-agnostic (no import from `ProjectDeepDive`).
  - **`effectiveMetrics(metrics, deliverables)` in `rampGroups.ts`** — returns metrics with bar-kind values rewritten to live deliverable %, mirroring `ProjectDeepDive`'s metrics-rewrite useEffect. **Without this, bar items used persisted (potentially stale) values in list views even when the user had toggled deliverables since the last save.** This was the second-pass bug we missed on the first attempt.
  - Wired both into `Dashboard.tsx`, `PortfolioHeatmap.tsx` (sort + every cell), `aiClient.ts` (`buildSnapshot`), and `ProjectDeepDive.tsx`'s `AIAnalysisPanel` projectInput (added `deliverables` to `AnalyzeProjectInput`).
- **Subtle bug found via console instrumentation.** First fix-pass still left a 9-point gap. Added `window.__bridgeDebug` exports to both Dashboard's `ProjectListItem` and `ProjectDeepDive` (rolledUp + every input), had user diff them in the browser console — the diff showed `deliverableScores` differing across many items even with identical `deliverables` and `metrics`. Root cause: the new `_deliverableCounts` helper in `rampGroups.ts` short-circuited with `if (!state) return { done: 0, total: 0 }`. ProjectDeepDive's original `deliverableCountsForItem` instead defaulted state to `{ checkedIds: [] }`, so an item with template deliverables defined in `rampGroups.ts` but no entry yet in `project.deliverables` STILL had `total > 0`, contributing 0% to the value-item blend (drags score down). The short-circuit version skipped that blend entirely → numeric-only score → list views looked artificially high. Fixed by removing the short-circuit so semantics match the deep-dive exactly. Debug instrumentation removed once verified.
- **Open follow-ups (after today):**
  1. **Deploy** `firestore.rules` (Meetings rules) + `firestore.indexes.json` (meetings composite index). Per the multi-DB lie gotcha, paste rules into the Console manually if `npm run deploy:rules` doesn't move the "Last published" timestamp. The Meetings list query won't render until the index hits "Enabled."
  2. **Push to Vercel** so the AI Analysis fixes (empty-scope guard + retry+fallback + leaked upstream error) take effect in prod. `npm run dev` has them already because Vite re-imports on save.
  3. **Internal rename** `MeetingsTool` → keep as-is for now (no rename pending — user-facing name "Meetings" matches internal). If the SaaS name later becomes "Project Meetings" or similar, treat like the BOMAnalyzer→AltBOMTool / DocGuardTool→Doc Guard precedents.
  4. **Wire Meetings into `RAMP_GROUPS` if it ever scores readiness** — today it's just a log. If we later want "Were the gate-readiness review meetings actually held?" to count, mirror the `taktSummary` writeback pattern from `TaktStudyTool` (write a snapshot to the project doc on save, surface as a header pill).
  5. **Tunable blend ratio.** The 50/50 in `scoreForItem` is a constant. If users want different metrics to weight deliverables differently, this becomes a per-item field on `RampSubItem` or a project-level "deliverable weight: 0–100%" setting.
  6. **Manual gate-UI toggle.** Gate visibility is `templateId`-driven today. If users want to override per-project, add `gatesOverride?: boolean` on the project doc + a toggle in General Info.
  7. Carry-over: same Doc Guard / takt-summary / Firebase Auth follow-ups from earlier sessions.

### 2026-05-12
- **New Academy article: DFx Methodology.** Second subject in `BridgeOpsAcademy.tsx` (after Gate Map). Lead paragraph + six pillar cards (DFA, DFM, DFC, DFT, DFS, DFSC — each with full name, one-liner, goal, distinct color, lucide icon: Layers / Factory / Coins / FlaskConical / Wrench / Truck) + a horizontally-scrolling **Discipline × Gate matrix** (6 rows × 6 gate columns CR → MP, one terse activity per cell) + a "how to use this in practice" footnote on parallel-not-sequential application. Gate column accents reuse the GateMap palette via a new `GATE_ACCENT` map keyed by `ProductGate` so the two articles visually align. New subject added to the `SUBJECTS` registry — one-line append, sidebar wired automatically.
- **New tool: PFMEA — Process Risk Analysis** (`PFMEATool.tsx`, ~930 lines). Per-project utility tab, sitting between **Meetings** and **History** in `ProjectDeepDive`'s secondary strip. Same list-view + form-view chrome as `MeetingsTool` / `TaktStudyTool` (slate-900 header, blue "New" button, ShieldAlert icon, AIAG-VDA 2019 tagline in the header eyebrow).
- **Standards alignment.** AIAG-VDA 2019 PFMEA — current replacement for the AIAG 4th edition + VDA 4 (2008). Built-in S/O/D anchor scales (1–10, ten-step descriptions per axis) are taken from the AIAG-VDA 2019 anchors. Each score selector exposes a hover tooltip with the full scale and highlights the currently-picked row in amber. RPN computed live (`S × O × D`, capped at 1000) and tiered Low <40 / Medium 40–100 / High >100 (conservative default — many companies tune to their own risk policy). Action Priority (H/M/L) computed via `actionPriority(s,o,d)` — a published simplification of the standard's 3D lookup table; close enough for prioritisation, teams with formal compliance overlay their own AP rules. Both RPN tier and AP chips render on every row, color-coded.
- **Risk row UX.** Each risk is a card with four bands: top bar (#index + RPN/Tier chip + AP chip + duplicate / delete), identification grid (Process Step / Failure Mode / Failure Effect), scoring strip (S | Cause | O | Controls | D — five-column responsive layout `[110px_1fr_110px_1fr_110px]`), action plan (Recommended Action / Responsible / Actions Taken). Card border + soft top-bar tint switch to rose/amber/emerald based on RPN tier so high-risk rows pop visually. Collapsible "Revised scores (after action)" panel at the bottom — when all three revised S/O/D are set, a black "Revised RPN N · Tier · AP" chip appears next to the original in the top bar. `×` clears a revised score (turns it back to undefined).
- **Header fields.** Process under analysis (200 char, required for save), Review date (UTC-anchored date input, same helper pattern as Meetings), Participants (500 char comma-separated, mirrors Meetings attendees), Scope/Boundaries (500 char). Summary chip strip below: total risks · max RPN · mean RPN · high/medium/low count.
- **Two diagrams.** (1) `Heatmap` — 10×10 Severity × Occurrence SVG grid, cells tinted by `S × O` baseline (≥50 rose, ≥20 amber, else emerald). Risk dots positioned at their (S, O) coordinate, **dot size scales with Detection** (1=small → 10=large) so weak controls amplifying a medium-tier risk visually pop. Multiple risks on the same cell collapse to one dot with a count badge; the dot uses the worst (max-RPN) risk for color. (2) `Pareto` — top 10 risks by RPN, horizontal bars (label trimmed to 36 char), color-coded by tier, RPN value to the right of each bar. Empty-state message when no risks scored.
- **Persistence: `pfmeas` Firestore collection.** `userId + projectId` scoped. Doc shape: `{ userId, projectId, dateMs, title, scope, participants, risks: RiskLine[], createdAt, updatedAt }`. Each `RiskLine` carries an id + the eleven fields above + three optional revised S/O/D. `isValidPFMEA` validator + `match /pfmeas/{pfmeaId}` block added to `firestore.rules` (always editable — `update` allowed, same posture as meetings). Composite index `(userId asc, projectId asc, dateMs desc)` added to `firestore.indexes.json`. Same multi-database deploy lie applies — paste into Console if `npm run deploy:rules` doesn't move the "Last published" timestamp.
- **Tab strip wiring.** Three-line append in `ProjectDeepDive.tsx`: added `ShieldAlert` to lucide imports, added `PFMEA_TAB_ID = '__pfmea__'` constant, appended the entry to the `utilityTabs` array around line 1600, added the rendering branch in the `AnimatePresence` block mirroring the MeetingsTool block. Five utility tabs now: AI Analysis · Studies · Meetings · PFMEA · History.
- **Severity tooltip alignment fix.** Initial build positioned all three S/O/D tooltips with `right-0` (anchor right edge → grow leftward). Worked for middle/right columns (Occurrence, Detection) since they have neighbors on the left, but the Severity tooltip overflowed leftward off the card. Added a `tooltipAlign?: 'left' | 'right'` prop to `ScoreSelect` (default `'left'` = legacy behavior), pass `'right'` only for the Severity instances (initial + revised). Tooltip className flips between `right-0` and `left-0`. Type-check clean.

- **New tool: Process Map** (`ProcessMapTool.tsx`, ~900 lines). Per-project Visio-style manufacturing workflow editor. Originally placed between PFMEA and History in the secondary strip; later same day moved behind the new Project Tools launcher (see UX refactor below).
- **Naming.** Considered "Process Flow", "PFD", "Manufacturing Workflow", "Process Map" — landed on **Process Map** (industry standard, pairs cleanly with PFMEA → completes the classic Process Map → PFMEA → Control Plan triad). Internal identifiers all match the user-facing name (file `ProcessMapTool.tsx`, collection `processMaps`, tab id `__process_map__`).
- **Data model.** Step list is the source of truth; manufacturing order = array order. Each step has `kind` (start / action / decision / end), `description` (≤200 char), `docRef` (≤30 char, e.g. "WI-014 §3.2"), and decision steps carry `yesNext` / `noNext` step ids. Unset decision targets fall through to next-in-list so the diagram never dead-ends silently. End steps are terminal (no outgoing edge). Top-level fields: `title` (200 char), `description` (500 char optional), `steps`, `manualPositions`, `layoutDirection` (TB / LR), `updatedAtMs`.
- **Diagram engine.** **`@xyflow/react` v12.3.5 + `dagre` v0.8.5** — best-in-class flow editor (used by n8n / Linear / Stripe). Custom node types for each kind: oval (start/end via `border-radius: 9999px`), rect (action), diamond (square rotated 45° with a counter-rotated inner content wrapper so text stays upright). Color palette: emerald (start), blue (action), amber (decision), rose (end). Edges: smoothstep with arrow markers, branch labels ("Yes"/"No") on decision outgoing edges.
- **Auto-layout.** dagre runs on (steps, edges, direction) → maps step id → top-left coords (dagre returns centers, we subtract half-w/h for React Flow). Memo'd so unrelated re-renders never re-layout. User can drag any node to override; the drag pushes coords into a `manualPositions: Record<stepId, {x,y}>` map only on drag-end (not per-pixel — keeps parent state quiet during drag). "Reset layout" button discards manual coords. Layout direction toggle (Top-down / Left-right) live.
- **Export.** PNG + SVG, both built **directly from dagre output** (not a DOM screenshot) — deterministic, doesn't depend on React Flow CSS being loaded, ~150 lines of `buildSvg()` glue. SVG includes title, arrow markers, branch label pills, doc refs baked into each shape, text wrapped via a small `wrapLines(text, maxChars, maxLines)` helper. PNG goes SVG → Blob → `Image` → `canvas.toBlob('image/png')` at 2x scale. Both downloaded via the same `downloadBlob` helper.
- **Persistence: `processMaps` Firestore collection.** `userId + projectId` scoped. Doc shape: `{ userId, projectId, title, description, steps: FlowStep[], manualPositions, layoutDirection, updatedAtMs, createdAt, updatedAt }`. Always editable (`update` allowed, mirrors meetings / pfmeas posture). `isValidProcessMap` validator + `match /processMaps/{id}` block in `firestore.rules`. Composite index `(userId asc, projectId asc, updatedAtMs desc)` in `firestore.indexes.json`. Same multi-DB deploy lie applies — paste rules + verify "Last published" in Console.
- **Sandbox install blocked.** `@xyflow/react` and `dagre` are forbidden by the sandbox npm registry policy (E403). Added them to `package.json` and asked user to `npm install` locally — they did, then surfaced a real type error: `NodeChange[]` (generic over `NodeBase`) wasn't assignable to `NodeChange<Node<NodeData>>[]` because xyflow v12 types `NodeChange` generic over the node's `data` type. Fixed by explicitly typing `useNodesState<Node<NodeData>>` + `useEdgesState<Edge>` + the `handleNodesChange` parameter as `NodeChange<Node<NodeData>>[]`. Also let me drop two `as any` casts on `c.dragging` / `c.position` that I'd defensively included before knowing the strict generic shape.
- **UX refactor: Project Tools launcher** (later same day). Secondary tab strip had grown to **6 items** (AI Analysis · Studies · Meetings · PFMEA · Process Map · History) and more sub-apps are inevitable (Control Plan, FRACAS, 8D, ECN tracker…). Rejected a generic "Advanced Features" dropdown — hides discoverability for new users who don't know what tools exist. Instead: **split by role**. Pinned tabs are *views over the project* (AI Analysis, History — they summarise the project itself). Sub-app workspaces (Studies, Meetings, PFMEA, Process Map, future tools) hide behind a single **"Project Tools"** launcher in the middle of the strip.
- **Launcher mechanics.** New `PROJECT_TOOLS` registry constant near the tab id constants — single source of truth for tile metadata (`{ id, label, description, icon, iconActiveClass, tileBg, tileIcon }`). Adding a new tool = one entry here + a matching `<AnimatePresence>` branch further down. The strip itself never grows.
- **Active state.** When a tool is open, the launcher button takes on that tool's identity — icon, label, and the active underline (`layoutId="deepdive-utility-underline"` so the underline animates smoothly between AI Analysis / launcher / History). Click the launcher again to switch tools without going back to a neutral state. When nothing is open, the button shows a generic `LayoutGrid` icon + "Project Tools" + chevron.
- **Popover.** Flat 2-column grid (not categorised — user explicitly asked for no ANALYZE / COMMS / PROCESS subheadings). Tile = color-tinted icon plate (matches tool's color family) + uppercase name + one-line description. Closes on outside click (mousedown listener on document via `useRef` + `useEffect` cleanup), Escape, or selection. `aria-haspopup="menu"` + `aria-expanded` on the launcher button, `role="menu"` + `role="menuitem"` on the popover and tiles.
- **Open follow-ups (after today):**
  1. **Deploy** `firestore.rules` (pfmeas rules) + `firestore.indexes.json` (pfmeas composite index) — same Console-paste fallback expected. PFMEA list view won't render until the new index hits "Enabled."
  2. **PFMEA xlsx export.** Mirror the `taktXlsx.ts` pattern — 2-sheet workbook (Summary + Risks rows with all 11 fields + revised columns + RPN/AP). Download button in both the open form and per-row in the list. Probably ~120 lines of SheetJS glue.
  3. **PFMEA → RampScore wiring.** On save, mirror a snapshot to `project.pfmeaSummary` (`{ maxRpn, highCount, mediumCount, totalRisks, completedAtMs }`) — analogous to `taktSummary` from `TaktStudyTool`. Surface a "Risk ●" pill in the project header pill row. Full integration into `RAMP_GROUPS` (so the max RPN actually moves the readiness score) is a follow-up beyond that.
  4. **AP lookup precision.** Current `actionPriority(s,o,d)` is a published approximation of the AIAG-VDA 2019 3D table. For teams that need exact compliance, swap in the full 125-cell lookup (or expose a hook so a per-project override can replace it). Probably a `AP_LOOKUP_TABLE` constant + a config flag.
  5. **Per-FMEA template.** Manufacturing PFMEA, Service / FRU FMEA, Software FMEA, Supplier FMEA each have slightly different field sets. v1 is process-only; adding a `kind` field + template-driven row shape would let one tool serve all four — similar to how `templateId` drives gate UI in `ProjectDeepDive`.
  6. **AI-assisted risk seeding.** Given the project's `productType` + a process step, ask Gemini to propose likely failure modes / effects / typical S anchors as suggestions the user accepts/rejects. Same Firebase-ID-token-verify pattern as the other AI handlers.
  7. **Internal rename consideration.** User-facing label is "PFMEA" — internal identifiers (file `PFMEATool.tsx`, collection `pfmeas`, tab id `__pfmea__`) all match. If we later add a Design FMEA tool, decide naming: split into `pfmeas` + `dfmeas` (separate collections + tools) vs. one `fmeas` collection with a `kind` discriminator (preferred per #5).
  8. **Deploy `processMaps` rules + index.** Same Console-paste fallback expected if `npm run deploy:rules` doesn't move the "Last published" timestamp. Process Map list query won't render until the new composite index hits "Enabled" in the Firebase Console — usually 1–3 min after deploy.
  9. **Process Map xlsx export.** Mirror the `taktXlsx.ts` pattern — 2-sheet workbook (Summary + Steps with all fields including yes/no targets resolved to step numbers). Download button alongside the existing PNG / SVG buttons. Probably ~80 lines of SheetJS glue.
  10. **Process Map → RampScore wiring.** On save, mirror a snapshot to `project.processMapSummary` (`{ mapId, title, stepCount, hasDecisions, completedAtMs }`) — analogous to `taktSummary` from `TaktStudyTool`. Surface a "Flow ●" pill in the project header pill row. Full RAMP_GROUPS integration (process maturity as a scored metric) is a follow-up beyond that.
  11. **Cross-feature glue: Process Map → PFMEA "Import steps".** Given a saved Process Map, one-click pre-seed PFMEA `processStep` fields from the action / decision steps. High-leverage — Process Map → PFMEA is the natural authoring sequence, and copy-paste between them today is needless friction.
  12. **AI-assist for Process Map.** Given productType + a draft step, ask Gemini for likely next-step suggestions or decision-branch failure points. Same Firebase-ID-token-verify pattern as the other AI handlers. Lower priority than #6 (PFMEA AI-assist) since Process Map authoring is faster than risk scoring already.
  13. **Project Tools launcher polish.** A few possible enhancements once more tools land: (a) keyboard shortcut (`Cmd+K` opens launcher); (b) "Recently used" row at the top of the popover; (c) usage hints (e.g., "Last opened 3 days ago" on each tile); (d) Cmd-click to open a tool in a new tab (would require route-per-tool, not a small change). None blocking, all icing.
  14. **Adding a 5th+ tool.** The `PROJECT_TOOLS` registry in `ProjectDeepDive.tsx` is the only file that needs to grow when adding a sub-app (plus the tool's own component + the matching `<AnimatePresence>` branch). The strip itself never changes shape. First candidates likely Control Plan, FRACAS, 8D, ECN tracker — see existing CLAUDE.md mentions.

### 2026-05-14
- **End-to-end review of the AI Tool** (UX/UI, architecture, security, hosting) before opening to external beta users. Two real blockers found and fixed today; rest of the findings (rate limiting, observability, mobile lockout, first-run tour, CSP, etc.) tracked as open follow-ups.
- **Step 1 — Closed the Gemini API key leak in the client bundle.** `RampScoreTool.tsx` (the public no-login `/#/ramp-score` lead-gen surface) was calling Gemini directly from the browser with `process.env.GEMINI_API_KEY`, which `vite.config.ts`'s `define` block was injecting into the production JS bundle. Anyone could `view-source` `/ramp-score` and extract the key. New flow:
  - New file `api/ramp-score-analyze.ts` — server-side handler. **Unauthenticated** by design (RampScore stays anonymous for lead-gen), but with defenses: in-memory per-IP rate limit (5 calls / 10 min, opportunistic GC at 1000 entries), structured-data input only (server owns the prompt template — clients can't inject), body-size caps on every input field, same retry + fallback ladder as `ai-analyze.ts` (`gemini-2.5-flash` → `gemini-2.5-flash-lite`), AIza key stripped from any leaked Gemini error before it reaches the client.
  - `RampScoreTool.tsx` — dropped the `@google/genai` SDK import + `GoogleGenAI` client call, switched to `fetch('/api/ramp-score-analyze')`. The offline static fallback (the 3-risk / 3-rec boilerplate at lines ~777-810) is preserved unchanged — now triggered by any non-200 from the endpoint instead of "no API key".
  - `vite.config.ts` — removed the `define` block that exposed `GEMINI_API_KEY` to the client. Verified zero `AIza` hits in the deployed bundle via `view-source` after deploy.
  - **Rotated the Gemini API key** in Google AI Studio. Created a new key for production, updated `.env.local` + Vercel env vars, deleted the old "BridgeOps Production" key. Old leaked key is now dead.
- **Step 2 — 2-user closed-beta allowlist** (replaces the hardcoded `ADMIN_EMAIL = 'ehakun1807@gmail.com'` everywhere). Trusted colleagues, short window, deleted at end of beta.
  - **Design choice:** instead of a Firestore-backed allowlist collection (3+ hours, future-proof but overkill for 2 users), inlined a hardcoded list. Email/password sign-in with manually-provisioned Firebase Auth Console users using fake-domain emails (`beta1@bridgeops.local` style). Allowlist is the gate; Firebase Auth is just the session-issuance layer.
  - **First attempt failed in production with `ERR_MODULE_NOT_FOUND`.** Centralized `ALLOWED_EMAILS` + `isAllowedEmail` in `config.ts`, had all 6 API handlers `import { isAllowedEmail } from '../config.ts';`. `tsc --noEmit` passed locally. In prod, Vercel's serverless bundler preserved the `.ts` extension literally in the deployed JS (`/var/task/api/ai-analyze.js` tried to resolve `/var/task/config.ts` at runtime, which Node can't find because the deployed copy is `.js`). All 6 endpoints 500'd on first call.
  - **Fix:** inlined the `ALLOWED_EMAILS` Set + `isAllowedEmail` helper directly in each of the 6 API handlers. Same self-contained pattern as the original `ADMIN_EMAIL` const that worked. `config.ts` still exists with the same array (client uses it via `AuthModal` and `App.tsx`'s dashboard gate) but the server handlers no longer cross-file-import. Each inline block carries a `// Closed-beta allowlist — KEEP IN SYNC` comment so grep can find all 8 copies.
  - **Tradeoff acknowledged:** allowlist now lives in **8 places** — `config.ts` + 6 API handlers + `firestore.rules`. For 2 testers, manageable. If the beta scales past 3-4 users, switch to a Vercel env var (`ALLOWED_EMAILS=...,...` comma-separated) parsed at module init — single edit point, no code redeploy on tester change.
  - **Dropped the `email_verified == true` requirement** from all 6 server-side gates AND from `firestore.rules`'s `isAdmin()`. Reason: Firebase Auth users created manually via the Console have `emailVerified=false` by default with no UI to flip it; verification only flips to `true` after the user clicks a Firebase-sent link, which can't reach a fake `@bridgeops.local` inbox. Since the allowlist is hardcoded and sign-up is disabled (`AuthModal` is sign-in-only), the verified flag isn't adding security — the allowlist already is the security boundary. Each handler + the rules file carries a comment noting "re-add `email_verified` before public sign-up." Critical reminder for end-of-beta.
  - `AuthModal.tsx` — added a small `text-[9px]` hint under "Continue with Google": "Beta testers — use the email + password issued to you above." Google button still works for admin Google sign-in; the hint just steers colleagues to the right path so they don't accidentally trigger the existing `rejectNonAdmin` auto-delete on their real Gmail.
  - 2 Firebase Auth users created manually in Firebase Console → Authentication → Users → Add user, with fake-domain emails matching `config.ts` + `firestore.rules`. Credentials shared with colleagues out-of-band (Signal).
  - **Both users smoke-tested in fresh incognito sessions:** sign-in lands on Dashboard (not Private Beta lockout), project create + open works (Firestore reads/writes), AI Analysis returns real Gemini output (server-side allowlist works).
- **Vercel + Firebase deploy notes from today:**
  - The multi-database deploy lie hit again on `firestore.rules` — `npm run deploy:rules` printed `✔ Deploy complete!` but the "Last published" timestamp in the Console didn't move. Manual paste into Console → Publish was required. Pre-existing gotcha, documented above.
  - `npm run dev` (Vite) does NOT serve `/api/*` routes — those are Vercel functions and require either `vercel dev` or a deployed env to actually run. Wasted ~10 min during step 1 testing on this. Worth documenting if a future contributor tries to test API handlers locally without `vercel dev`.
- **Open follow-ups (after today):**
  1. **End-of-beta cleanup checklist** — when the beta wraps: (a) delete the 2 Firebase Auth users from the Console; (b) remove their emails from `config.ts`, all 6 API handlers, and `firestore.rules`; (c) **re-add `email_verified == true` to all server-side gates AND `firestore.rules`'s `isAdmin()`** before opening to any public sign-up — the comments in each file mark exactly where; (d) delete any Firestore docs they created across `users`, `projects`, `scores`, `tasks`, `taktStudies`, `meetings`, `pfmeas`, `processMaps`, `docGuardAudits` (search by `userId`).
  2. **Per-UID rate limiting on the 5 authenticated AI handlers.** The new `/api/ramp-score-analyze` got IP rate limiting on day one; the existing 5 handlers (`ai-analyze`, `ai-coach`, `docguard`, `find-equivalent`, `quote-compare`) don't. With 3 trusted users it's not urgent, but the next blocker on opening up. Implement via Firestore (`rateLimits/{uid}` doc with count + windowStart) to avoid bringing in Upstash/Redis — keeps the "no paid services" constraint.
  3. **Sentry observability** (free Developer tier: 5k errors/month). Currently flying blind beyond Vercel function logs. Cheap insurance before scaling.
  4. **Mobile lockout screen** on `Dashboard` + `ProjectDeepDive` — at small viewport, render a friendly "open this on desktop" gate. The dashboard is 4k lines with ~15 responsive breakpoints; mobile UX is broken today.
  5. **First-run tour / empty-state copy** on Dashboard. New users won't know what RampScore / PFMEA / Doc Guard / Alt BOM mean without context. 15-min screenshare with each beta user is a workable substitute for now.
  6. **`docguard.ts` retry + fallback ladder** — it's the last handler still without the pattern. Port from `ai-analyze.ts`. ~20-min change.
  7. **CSP header in `vercel.json`** — basic Content-Security-Policy blocks classes of XSS. Hygiene, not active bleeding.
  8. **Disable Email/Password provider** in Firebase Console **after** the beta is over (and the 2 fake users are deleted). Google-only reduces attack surface.
  9. **Move allowlist to a Vercel env var** if expanding beyond 2-3 testers. Avoids the 8-places-to-edit pain. Already sketched above.
  10. **Carry-over from prior sessions** — RAMP_GROUPS wiring for `taktSummary` / `pfmeaSummary` / `processMapSummary` mirrors, internal renames of `BOMAnalyzerTool` → `AltBOMTool` and `DocGuardTool` → `Doc Guard`, Process Map → PFMEA "Import steps" glue, etc. None blocked by today's work.
